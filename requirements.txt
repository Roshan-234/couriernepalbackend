# MoonlightCRM backend — production dependencies (pinned).
# Install on cPanel via the Python App's virtualenv:
#   pip install -r requirements.txt
# Transitive deps (aniso8601, alembic, blinker, itsdangerous, click, etc.)
# are pulled automatically; only direct deps + a few load-bearing pins listed.

Flask==3.1.1
Werkzeug==3.1.3
Jinja2==3.1.6
itsdangerous==2.2.0

Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.43
Flask-Migrate==4.1.0

Flask-JWT-Extended==4.7.1
PyJWT==2.10.1
Flask-Bcrypt==1.0.1
bcrypt==4.3.0

Flask-Limiter==3.12
Flask-Mail==0.10.0
flask-cors==6.0.1
flask-restx==1.3.0
flask-swagger-ui==5.21.0

marshmallow==4.0.0
python-slugify==8.0.4
python-dotenv==1.1.1

# MySQL driver used by ProductionConfig (mysql+pymysql://)
PyMySQL==1.1.1
